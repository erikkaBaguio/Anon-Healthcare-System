create table User (
	id INT PRIMARY KEY,
	fname varchar(45),
	mname varchar(45),
	lname varchar(45),
	email varchar(45),
	is_active boolean
);

----------------------------------------------------------------------------------------------------

