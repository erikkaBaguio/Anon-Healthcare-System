from lettuce import step

@step(u'Given admin is not logged in')
def given_admin_is_not_logged_in(step):
    assert False, 'This step must be implemented'

@step(u'And inputs username and password')
def and_inputs_username_and_password(step):
    assert False, 'This step must be implemented'

@step(u'When the admin clicks login button')
def when_the_admin_clicks_login_button(step):
    assert False, 'This step must be implemented'

@step(u'Then the admin will be redirected to dashboard')
def then_the_admin_will_be_redirected_to_dashboard(step):
    assert False, 'This step must be implemented'

@step(u'Given doctor is not logged in')
def given_doctor_is_not_logged_in(step):
    assert False, 'This step must be implemented'

@step(u'And inputs the login form')
def and_inputs_the_login_form(step):
    assert False, 'This step must be implemented'

@step(u'When the doctor clicks login button')
def when_the_doctor_clicks_login_button(step):
    assert False, 'This step must be implemented'

@step(u'Then the doctor redirected to dashboard')
def then_the_doctor_redirected_to_dashboard(step):
    assert False, 'This step must be implemented'

@step(u'Given nurse is not logged in')
def given_nurse_is_not_logged_in(step):
    assert False, 'This step must be implemented'

@step(u'When the nurse clicks login button')
def when_the_nurse_clicks_login_button(step):
    assert False, 'This step must be implemented'
    
@step(u'Then the nurse redirected to dashboard')
def then_the_nurse_redirected_to_dashboard(step):
    assert False, 'This step must be implemented'
