from lettuce import steps
from nose.tools import *

@step(u'Given the patient is not yet registered in the system.')
def given_the_patient_is_not_yet_registered_in_the_system(step):
    assert False, 'This step must be implemented'

@step(u'And the admin fills up the account form')
def and_the_admin_fills_up_the_account_form(step):
    assert False, 'This step must be implemented'

@step(u'When the admin clicks the save button.')
def when_the_admin_clicks_the_save_button(step):
    assert False, 'This step must be implemented'

@step(u'Then the system will display a success message that states that a new accout of a patient is saved.')
def then_the_system_will_display_a_success_message_that_states_that_a_new_accout_of_a_patient_is_saved(step):
    assert False, 'This step must be implemented'